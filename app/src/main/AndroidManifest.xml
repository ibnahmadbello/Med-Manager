<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.regent.tech.med_manager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:label="@string/login_page"/>
        <activity android:name=".WelcomeActivity" android:label="@string/welcome"
            android:parentActivityName=".LoginActivity"/>
        <activity android:name=".AddMedicationActivity" android:label="@string/add_medication"
            android:parentActivityName=".WelcomeActivity"></activity>
    </application>

</manifest>